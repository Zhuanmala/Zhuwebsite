# Zhuwebsite 项目总结

更新时间：2026-05-10

## 项目定位

这是朱佳乐的个人作品集网站，当前线上域名：

- https://zhujiale.art
- `www.zhujiale.art` 会跳转到主域名

网站定位是舞台和服装设计师作品集。当前设计基准是原来的黑底、白色细线、几何装饰、作品网格与弹窗轮播，不要在小改动中随意替换整体视觉结构。

## 技术栈

- React
- TypeScript
- Vite
- Tailwind CSS
- Framer Motion / `motion`
- Lucide React 图标

本地主要项目目录：

```text
D:\Work\Zhuwebsite-deploy\Zhuwebsite-main
```

用于 Git 提交和推送的工作副本：

```text
D:\Work\Zhuwebsite-git-work
```

线上仓库：

```text
https://github.com/Zhuanmala/Zhuwebsite
```

## 部署方式

项目通过 GitHub Actions 自动部署到 GitHub Pages。

工作流文件：

```text
.github/workflows/deploy-pages.yml
```

部署流程：

1. 推送到 `main` 分支
2. GitHub Actions 执行 `npm install`
3. 执行 `npm run build`
4. 使用 `peaceiris/actions-gh-pages@v4` 将 `dist` 发布到 `gh-pages`
5. 写入 CNAME `zhujiale.art`

构建命令：

```bash
npm run build
```

本机没有系统 Git 时，可使用 portable Git：

```text
C:\Users\houji\AppData\Local\OpenClaw\deps\portable-git\mingw64\bin\git.exe
```

## DNS 与域名

域名注册商：Porkbun

当前域名：

```text
zhujiale.art
```

DNS 已配置到 GitHub Pages：

- Apex/root 使用 GitHub Pages 的 A 记录
- `www` 使用 CNAME 指向 `zhuanmala.github.io`

GitHub Pages 自定义域名检查已通过。

## 关键文件

入口文件：

```text
src/main.tsx
```

当前应导入原版页面：

```tsx
import App from "./app/App.tsx";
```

核心页面文件：

```text
src/app/App.tsx
```

作品集数据、双语文案、轮播图片数组、联系方式、About 内容、联系弹窗和作品弹窗交互都在这个文件中。

不要再切换到 `AppV2.tsx`。此前 `AppV2.tsx` 是一次过度改版尝试，会改变页面整体风格；当前线上设计应以 `App.tsx` 为准。

## 视觉与交互原则

用户已明确要求：

- 保留原来的简单线条、黑底、几何装饰风格
- 不要在主页名字旁边放作品集图片
- 后续优化尽量只改必要内容
- 不要随意重做整体布局

作品弹窗当前交互：

- 默认自动轮播
- 支持左右按钮手动切换
- 支持圆点切换
- 支持暂停 / 继续自动轮播
- 点击当前图片可自适应放大查看
- 放大图使用 `object-contain`，避免竖图或完整构图被裁切
- 放大图可通过右上角关闭按钮或点击背景关闭

作品卡片封面：

- 使用每个项目的 `image` 字段
- 弹窗轮播使用 `images` 字段

## 当前作品集顺序

作品区当前顺序：

1. Tranquilla Trampeltreu / Der Lindwurm und der Schmetterling
2. Orphous in der Unterwelt
3. Panic:Attacks
4. Rausch
5. Die Zauberflote
6. Xing Han Yun Yi / 中文：星汉云逸
7. Cha Yu Fan Hou / 中文：茶余饭后
8. Hong Gao Liang / 中文：红高粱

中文标题通过 `titleZh` 字段实现：

```tsx
const getProjectTitle = (project: Project, language: Language) =>
  language === "zh" && project.titleZh ? project.titleZh : project.title;
```

默认语言是英文：

```tsx
const [language, setLanguage] = useState<Language>("en");
```

## 作品图片目录映射

所有线上静态图片放在 `public` 下。Vite 构建后会直接发布这些路径。

### Tranquilla

```text
public/tranquilla/
```

当前 10 张：

```text
/tranquilla/01.jpg
...
/tranquilla/10.jpg
```

当前封面：

```tsx
image: tranquillaGalleryImages[0]
```

### Orphous in der Unterwelt

```text
public/unterwelt/
```

当前轮播顺序在 `unterweltGalleryImages` 中，不是简单文件名顺序：

```tsx
const unterweltGalleryImages = [
  "/unterwelt/05.jpg",
  "/unterwelt/06.jpg",
  "/unterwelt/01.jpg",
  "/unterwelt/08.jpg",
  "/unterwelt/04.jpg",
  "/unterwelt/09.jpg",
  "/unterwelt/03.jpg",
  "/unterwelt/02.jpg",
];
```

当前封面：

```tsx
image: unterweltGalleryImages[2]
```

注意：

- `/unterwelt/06.jpg` 是“人间2.3.4.JPG”
- `/unterwelt/09.jpg` 是“地狱2.3.4.JPG”

### Panic:Attacks

```text
public/panic-attacks/
```

当前封面：

```tsx
image: panicGalleryImages[1]
```

### Rausch

```text
public/rausch/
```

当前已删除原第 2 张，不使用 `/rausch/02.jpg`。

当前轮播：

```tsx
const rauschGalleryImages = [
  "/rausch/03.jpg",
  "/rausch/01.jpg",
  "/rausch/04.jpg",
  "/rausch/05.jpg",
  "/rausch/06.jpg",
];
```

### Die Zauberflote

```text
public/zauber/
```

当前两张：

```text
/zauber/01.jpg
/zauber/02.jpg
```

### Xing Han Yun Yi

```text
public/xing-han-yun-yi/
```

当前四张：

```text
/xing-han-yun-yi/01.jpg
/xing-han-yun-yi/02.jpg
/xing-han-yun-yi/03.jpg
/xing-han-yun-yi/04.jpg
```

其中：

- `01.jpg` 已替换为新版 `BHQY7408.JPG`
- `04.jpg` 是新增 `IMG_1759.JPG`

### Cha Yu Fan Hou

```text
public/cha-yu-fan-hou/
```

当前四张为 PNG：

```text
/cha-yu-fan-hou/01.png
/cha-yu-fan-hou/02.png
/cha-yu-fan-hou/03.png
/cha-yu-fan-hou/04.png
```

旧的 jpg 已删除。

### Hong Gao Liang

```text
public/hong-gao-liang/
```

当前三张：

```text
/hong-gao-liang/01.jpg
/hong-gao-liang/02.jpg
/hong-gao-liang/03.jpg
```

当前封面：

```tsx
image: hongGaoLiangGalleryImages[1]
```

## 作品内容维护方式

作品都在 `src/app/App.tsx` 的 `projects` 数组里。

每个作品结构：

```tsx
{
  id: number,
  title: string,
  titleZh?: string,
  subtitle: {
    zh: string,
    en: string,
  },
  year: string,
  venue: {
    zh: string,
    en: string,
  },
  image: string,
  images?: string[],
  description: {
    zh: string,
    en: string,
  },
}
```

约定：

- `title` 是英文标题
- `titleZh` 是中文界面显示标题，可选
- `subtitle` 统一写朱佳乐负责的工作，例如 `Costume Design · Jiale Zhu`
- `year` 显示年份
- `venue` 显示地点或机构
- `image` 是作品卡片封面
- `images` 是弹窗轮播图
- `description` 是弹窗里的详细说明

## 联系方式

联系方式在 `src/app/App.tsx` 的 Contact Section 中。

当前页面只显示：

- 邮件图标与 `Send email`
- 地址 `Shanghai / Munich`

当前页面不显示真实邮箱号，也不显示手机号。

邮箱图标不是 emoji，而是自定义线条 SVG，使用 `currentColor` 继承网站整体黑白线条风格。SVG 路径以此开头：

```text
M 2.00 7.67 C 2.00 7.11 ...
```

点击邮箱入口会打开站内联系弹窗。弹窗包含：

- From 输入框
- Message 文本框
- Cancel 按钮
- Send 按钮

发送方式：

- 当前没有后端服务
- `Send` 会通过 `mailto:` 调起用户本机默认邮件客户端
- 收件邮箱在代码中用数组拼接，避免真实邮箱直接出现在页面文本或打包后的直观文案中

相关代码逻辑：

```tsx
const recipient = ["zhujiale0208", "icloud.com"].join("@");
window.location.href = `mailto:${recipient}?subject=${subject}&body=${body}`;
```

## About 内容

About 板块在 `content.zh.about` 和 `content.en.about` 中。

当前新增奖项：

```text
动画电影《A Hamlet Machine》获 2022 年全球国际大学生影响力电影节优秀奖。
```

英文：

```text
International award: the animated film A Hamlet Machine received the Excellence Award at the 2022 Global International Student Impact Film Festival.
```

## 常见修改流程

### 1. 替换某个作品图片

步骤：

1. 将新图片复制到对应 `public/<作品目录>/`
2. 如果文件扩展名改变，比如 jpg 改 png，同步修改 `App.tsx` 里的图片路径
3. 运行构建：

```bash
npm run build
```

4. 同步到 Git 工作副本 `D:\Work\Zhuwebsite-git-work`
5. 提交并推送
6. 等 GitHub Actions 和 Pages 发布完成
7. 用线上 URL 验证图片返回 200

### 2. 改封面

只改 `projects` 中对应作品的 `image` 字段。

例如将封面设为轮播第 3 张：

```tsx
image: someGalleryImages[2]
```

### 3. 改轮播顺序

只调整对应 `xxxGalleryImages` 数组顺序。

注意：数组下标从 0 开始。

### 4. 新增作品

步骤：

1. 在 `public` 下新增作品图片目录
2. 新增对应 `xxxGalleryImages`
3. 在 `projects` 数组中新增对象
4. 保持 `id` 唯一
5. 构建、提交、推送、验证

## Git 操作参考

portable Git 路径：

```powershell
$git = 'C:\Users\houji\AppData\Local\OpenClaw\deps\portable-git\mingw64\bin\git.exe'
```

提交示例：

```powershell
& $git -C 'D:\Work\Zhuwebsite-git-work' add src/app/App.tsx public/<目录>
& $git -C 'D:\Work\Zhuwebsite-git-work' commit -m 'Update portfolio content'
& $git -C 'D:\Work\Zhuwebsite-git-work' push origin main
```

注意：

- `D:\Work\Zhuwebsite-git-work` 里可能有未跟踪的 `dist/`、`node_modules/`、`package-lock.json`
- 不要误提交这些无关文件
- 只 `add` 需要的文件

## 线上验证方法

检查最新 JS 是否包含改动：

```powershell
$html = (Invoke-WebRequest -Uri 'https://zhujiale.art/?cache_bust=提交号' -UseBasicParsing).Content
$asset = [regex]::Match($html, 'assets/[^"'']+\.js').Value
$js = (Invoke-WebRequest -Uri "https://zhujiale.art/$asset" -UseBasicParsing).Content
$js.Contains('要检查的文字')
```

检查图片是否在线：

```powershell
Invoke-WebRequest -Uri 'https://zhujiale.art/cha-yu-fan-hou/01.png' -UseBasicParsing -Method Head
```

返回 `200` 即正常。

最近一次与联系区相关的线上验证重点：

- 打包 JS 中包含邮件 SVG 路径
- 页面不包含 `📧`
- 页面不直观暴露真实邮箱文本

## 回滚建议

如果后续改坏了：

1. 优先回滚最近一次 commit
2. 或恢复 `src/main.tsx` 指向 `./app/App.tsx`
3. 不要切换到 `AppV2.tsx`
4. 不要重做整体 UI，除非明确要求

当前设计基准：

- `src/app/App.tsx`
- 黑底
- 白色细线 / 几何装饰
- 作品网格
- 弹窗轮播
- 默认英文界面
- 邮件入口为线条 SVG 图标

## 后续优化注意事项

- 所有文字改动优先同时改中英文
- 作品标题中文化用 `titleZh`
- 图片路径放在 `public` 下，引用时以 `/目录/文件名` 开头
- 改图片扩展名时，代码路径也必须同步改
- 封面和轮播可以用同一批图片，但由 `image` 和 `images` 分别控制
- 联系弹窗当前是 `mailto:` 方案，如果未来需要网页内真正发送邮件，需要新增后端或第三方表单服务
- 用户很在意不要破坏原设计，改动要小而准
